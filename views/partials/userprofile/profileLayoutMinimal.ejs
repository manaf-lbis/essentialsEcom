
<style>
    /* Clean Minimal Profile Design */
    body {
        background: #f8f9fa;
        font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        /* Prefer Inter if available */
        /* Padding for fixed navbar - roughly header height + extra space */
        min-height: 100vh;
    }

    .profile-container {
        max-width: 1200px;
        margin: 3rem auto;
        padding: 0 1.5rem;
    }

    .profile-layout {
        display: grid;
        grid-template-columns: 280px 1fr;
        gap: 2.5rem;
        align-items: start;
    }

    /* Modern Sidebar Styles */
    .profile-sidebar {
        background: #ffffff;
        border-radius: 16px;
        border: none;
        /* Removed border for cleanliness */
        padding: 2rem;
        height: fit-content;
        position: sticky;
        top: 100px;
        /* Adjusted for sticky navbar */
        z-index: 90;
        box-shadow: 0 10px 40px -10px rgba(0, 0, 0, 0.06);
        /* Soft, deep shadow */
        transition: all 0.3s ease;
    }

    .profile-sidebar .sidebar-header {
        padding-bottom: 1.5rem;
        border-bottom: 1px solid #f1f3f5;
        margin-bottom: 1.5rem;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
    }

    .profile-sidebar .sidebar-header h5 {
        margin: 0 0 0.5rem 0;
        font-size: 1.4rem;
        font-weight: 800;
        /* Bolder for title */
        color: #1a1a1a;
        letter-spacing: -0.5px;
        line-height: 1.2;
    }

    .sidebar-user-greeting {
        color: #868e96;
        font-size: 0.75rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 1.5px;
        margin-bottom: 0.25rem;
    }

    .sidebar-user-name {
        /* Name is handled by h5 usually, or just below */
        display: none;
        /* Hiding duplicate if not needed, or style it */
    }

    .sidebar-user-email {
        color: #adb5bd;
        font-size: 0.9rem;
        margin-top: 0.25rem;
    }

    .sidebar-nav {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    .sidebar-nav a {
        padding: 0.85rem 1.25rem;
        color: #495057;
        text-decoration: none;
        border-radius: 12px;
        transition: all 0.2s ease;
        font-size: 0.95rem;
        font-weight: 500;
        display: flex;
        align-items: center;
        gap: 12px;
        position: relative;
        overflow: hidden;
        /* Keep effects inside */
    }

    .sidebar-nav a:hover {
        background: #f8f9fa;
        color: #212529;
    }

    .sidebar-nav a.active {
        background: #f1f3f5;
        color: #000;
        font-weight: 700;
    }

    /* Cleaner active indicator dot instead of bar */
    .sidebar-nav a.active::after {
        content: '';
        position: absolute;
        left: 0;
        top: 50%;
        transform: translateY(-50%);
        height: 50%;
        width: 4px;
        background: #212529;
        border-radius: 0 4px 4px 0;
    }

    /* Profile Content Card - Matching the clean theme */
    .profile-content-card {
        background: #ffffff;
        border-radius: 16px;
        border: none;
        padding: 2.5rem;
        /* Restored padding for non-transparent cards */
        box-shadow: 0 10px 40px -10px rgba(0, 0, 0, 0.05);
        /* Added shadow for consistency */
    }

    /* Responsive Adjustments */
    @media (max-width: 992px) {
        .profile-layout {
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        .profile-sidebar {
            position: relative;
            top: 0;
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .sidebar-nav {
            flex-direction: row;
            flex-wrap: wrap;
            justify-content: center;
            width: 100%;
        }

        .sidebar-nav a {
            padding: 0.75rem 1rem;
            font-size: 0.9rem;
            background: #fff;
            border: 1px solid #f0f0f0;
        }

        .sidebar-nav a.active::before {
            display: none;
            /* Remove indicator on mobile horizontal layout */
        }

        .sidebar-header {
            width: 100%;
            margin-bottom: 1rem;
            padding-bottom: 1rem;
        }
    }
</style>

<aside class="profile-sidebar">
    <div class="sidebar-header">
        <h5>My Account</h5>
        <p class="sidebar-user-greeting">Hello,</p>
        <p class="sidebar-user-name">
            <%= userData.name%>
        </p>
        <p class="sidebar-user-email">
            <%= userData.email ? userData.email.substring(0,20)+'...' : userData.phone%>
        </p>
    </div>

    <nav class="sidebar-nav">
        <a href="/profile" class="<%= activePage === 'profile' ? 'active' : ''%>">
            <i class="fas fa-user mb-1" style="width: 20px;"></i> Profile
        </a>
        <a href="/address" class="<%= activePage === 'address' ? 'active' : ''%>">
            <i class="fas fa-map-marker-alt mb-1" style="width: 20px;"></i> Manage Address
        </a>
        <a href="/resetPassword" class="<%= activePage === 'resetPassword' ? 'active' : ''%>">
            <i class="fas fa-key mb-1" style="width: 20px;"></i> Reset Password
        </a>
        <a href="/referrals" class="<%= activePage === 'referrals' ? 'active' : ''%>">
            <i class="fas fa-gift mb-1" style="width: 20px;"></i> Referrals
        </a>
        <a href="/orders" class="<%= activePage === 'orders' ? 'active' : ''%>">
            <i class="fas fa-shopping-bag mb-1" style="width: 20px;"></i> My Orders
        </a>
        <a href="/walletLedger" class="<%= activePage === 'walletLedger' ? 'active' : ''%>">
            <i class="fas fa-wallet mb-1" style="width: 20px;"></i> Wallet
        </a>
    </nav>
</aside>
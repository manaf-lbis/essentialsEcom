<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("../partials/htmlHead.ejs",{csspath:'/css/adminCss/loginpage/login.css'})%>
        <style>
            /* Embedded critical CSS for structure if external file update lags */
            .login-wrapper {
                background-color: #f8fafc;
                display: flex;
                align-items: center;
                justify-content: center;
                min-height: 100vh;
            }
        </style>
</head>

<body class="bg-light d-flex align-items-center justify-content-center min-vh-100 p-3">

    <div class="row g-0 w-100 shadow-lg rounded-4 overflow-hidden bg-white"
        style="max-width: 900px; min-height: 500px;">

        <div class="col-md-6 d-none d-md-flex flex-column align-items-center justify-content-center p-5 text-white"
            style="background: linear-gradient(135deg, #0f172a 0%, #334155 100%);">
            <img src="/assets/images/logo.png" alt="Logo" class="img-fluid mb-4"
                style="max-height: 80px; filter: brightness(0) invert(1);">
            <h4 class="fw-light text-center ls-1">Smart Choice for Smart Living</h4>
            <p class="small text-white-50 mt-2">Admin Dashboard Panel</p>
        </div>

        <div class="col-md-6 d-flex align-items-center p-5 bg-white">
            <div class="w-100">
                <div class="d-md-none text-center mb-4">
                    <img src="/assets/images/logo.png" alt="Logo" height="50">
                </div>

                <h3 class="fw-bold mb-1 text-dark">Welcome Back</h3>
                <p class="text-muted small mb-4">Sign in to manage your store</p>

                <%if(typeof message !=='undefined' ){%>
                    <div class="alert alert-danger py-2 small border-0 bg-danger-subtle text-danger" role="alert">
                        <i class="fas fa-exclamation-circle me-1"></i>
                        <%= message%>
                    </div>
                    <%}%>

                        <form action="/admin" method="post">
                            <div class="form-floating mb-3">
                                <input type="email" name="email" class="form-control" id="floatingEmail"
                                    placeholder="name@example.com" required>
                                <label for="floatingEmail">Email address</label>
                            </div>
                            <div class="form-floating mb-4">
                                <input type="password" name="password" class="form-control" id="floatingPassword"
                                    placeholder="Password" required>
                                <label for="floatingPassword">Password</label>
                            </div>

                            <button type="submit" class="btn btn-dark w-100 py-2 fw-medium">Sign In</button>
                        </form>

                        <div class="mt-4 text-center">
                            <small class="text-muted">&copy; Essentials Admin</small>
                        </div>
            </div>
        </div>
    </div>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
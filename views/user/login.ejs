<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Smart Choice</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Catamaran:wght@100..900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <link rel="stylesheet" href="/css/userCss/loginPage/login.css">
    <style>
        /* Critical styles for layout */
        body {
            font-family: 'Catamaran', sans-serif;
            background-color: #f1f5f9;
        }

        .login-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .login-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
            width: 100%;
            max-width: 950px;
            min-height: 600px;
        }

        .brand-section {
            background: linear-gradient(135deg, #FF6B6B 0%, #FF8E53 100%);
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 40px;
        }

        .form-section {
            padding: 50px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .btn-google {
            border: 1px solid #e2e8f0;
            background: white;
            color: #475569;
            font-weight: 500;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            width: 100%;
            padding: 10px;
            border-radius: 8px;
            transition: all 0.2s;
            text-decoration: none;
        }

        .btn-google:hover {
            background: #f8fafc;
            color: #1e293b;
            border-color: #cbd5e1;
        }

        .divider {
            display: flex;
            align-items: center;
            text-align: center;
            color: #94a3b8;
            margin: 20px 0;
        }

        .divider::before,
        .divider::after {
            content: '';
            flex: 1;
            border-bottom: 1px solid #e2e8f0;
        }

        .divider::before {
            margin-right: .5em;
        }

        .divider::after {
            margin-left: .5em;
        }

        .form-control:focus {
            box-shadow: 0 0 0 3px rgba(255, 107, 107, 0.2);
            border-color: #FF6B6B;
        }

        .btn-primary {
            background-color: #FF6B6B;
            border: none;
            padding: 12px;
            font-weight: 600;
            border-radius: 8px;
        }

        .btn-primary:hover {
            background-color: #ff5252;
        }
    </style>
</head>

<body>

    <div class="login-container">

        <div class="position-fixed top-0 end-0 p-3" style="z-index: 1050">
            <%if (typeof message !=='undefined' ) {%>
                <div class="alert alert-success alert-dismissible fade show shadow-sm" role="alert">
                    <%= message%>
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
                <%}%>
        </div>

        <div class="row g-0 login-card">

            <div class="col-md-6 brand-section d-none d-md-flex">
                <img src="/assets/images/logo.png" alt="Logo" class="img-fluid mb-4"
                    style="max-height: 100px; filter: brightness(0) invert(1);">
                <h2 class="fw-bold mb-2">Smart Choice</h2>
                <p class="text-white-50 text-center px-4">Experience smart living with our curated collection of
                    essential products.</p>
            </div>

            <div class="col-md-6 form-section">
                <div class="text-center d-md-none mb-4">
                    <img src="/assets/images/logo.png" height="50" alt="Logo">
                </div>

                <h3 class="fw-bold mb-1">Welcome Back</h3>
                <p class="text-muted mb-4 small">Please enter your details to sign in</p>

                <form id="form" action="/login" method="post">
                    <%if(typeof error !=='undefined' ){%>
                        <div class="alert alert-danger py-2 small border-0 bg-danger-subtle text-danger mb-3">
                            <i class="fas fa-circle-exclamation me-2"></i>
                            <%= error%>
                        </div>
                        <%}%>

                            <div class="mb-3">
                                <label class="form-label small fw-bold text-muted">Email Address</label>
                                <input type="email" name="email" class="form-control" id="emailField"
                                    placeholder="name@example.com">
                                <div id="emailerr" class="text-danger small mt-1"></div>
                            </div>

                            <div class="mb-2">
                                <label class="form-label small fw-bold text-muted">Password</label>
                                <input type="password" name="password" class="form-control" id="passwordField"
                                    placeholder="Enter your password">
                                <div id="passworderr" class="text-danger small mt-1"></div>
                            </div>

                            <div class="text-end mb-4">
                                <a href="/forgotPassword"
                                    class="text-decoration-none small text-muted hover-link">Forgot Password?</a>
                            </div>

                            <div class="d-flex gap-2">
                                <button type="submit" class="btn btn-primary flex-grow-1">Sign In</button>
                                <button type="button" class="btn btn-outline-secondary" style="width: 50px;"
                                    data-bs-toggle="tooltip" data-bs-placement="top" title="Test Credentials"
                                    onclick="fillTestCredentials()">
                                    <i class="fas fa-lock"></i>
                                </button>
                            </div>

                            <script>
                                function fillTestCredentials() {
                                    document.getElementById('emailField').value = 'user@gmail.com';
                                    document.getElementById('passwordField').value = 'Login@121';
                                }

                                document.addEventListener('DOMContentLoaded', function () {
                                    var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle=\'tooltip\']'))
                                    var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                                        return new bootstrap.Tooltip(tooltipTriggerEl)
                                    })
                                });
                            </script>

                            <div class="divider small">OR</div>

                            <a href="/auth/google" class="btn-google mb-4">
                                <img src="/assets/images/google.webp" alt="Google" width="20">
                                <span>Continue with Google</span>
                            </a>

                            <div class="text-center">
                                <span class="text-muted small">New to our platform?</span>
                                <a href="/signup" class="fw-bold text-decoration-none text-dark ms-1">Create an
                                    account</a>
                            </div>
            </div>
        </div>
    </div>

    <script src="https://kit.fontawesome.com/your-code.js" crossorigin="anonymous"></script>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/userjs/login.js"></script>
</body>

</html>
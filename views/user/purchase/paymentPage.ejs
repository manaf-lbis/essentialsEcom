<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complete Payment</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f7fc;
            margin: 0;
            padding: 0;
        }
        .payment-card {
            width: 100%;
            max-width: 500px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
            padding: 30px;
            text-align: center;
        }
        .payment-card-header {
            font-size: 1.75rem;
            font-weight: bold;
            color: #2c3e50;
            margin-bottom: 30px;
        }
        .payment-method-info {
            font-size: 1.2rem;
            font-weight: 500;
            color: #007bff;
            margin-bottom: 20px;
        }
        .total-amount {
            font-size: 2.5rem;
            font-weight: 700;
            color: #28a745;
            margin-top: 20px;
        }
        .btn-complete {
            width: 100%;
            padding: 12px;
            font-size: 1.2rem;
            background-color: #007bff;
            border: none;
            border-radius: 8px;
            color: white;
            margin-top: 30px;
            transition: background-color 0.3s ease;
        }
        .btn-complete:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <div class="container d-flex justify-content-center align-items-center min-vh-100">
        <div class="payment-card">
            <div class="payment-card-header">
                Complete Your Payment
            </div>
            
            <!-- Payment Method Information -->
            <div class="payment-method-info">
                <strong>Payment Method:</strong> <span id="paymentMethod">Online Payment</span>
            </div>

            <!-- Total Amount -->
            <div class="total-amount">
                ₹<%=finalAmount.toFixed(2)  %>
            </div>

            <!-- Complete Payment Button -->
            <button class="btn-complete" id="payNowBtn" razorpayKey="<%=razorpayKey  %>"  razorpayOrderId="<%= razorpayOrder.id %>" finalAmount="<%= finalAmount %>" >
                Complete Payment
            </button>
        </div>
    </div>

  
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
    <script src="/js/userjs/purchase/paymentpage.js"></script>

</body>
</html>

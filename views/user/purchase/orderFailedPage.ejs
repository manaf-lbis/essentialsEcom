<!DOCTYPE html>
<html lang="en">

<head>
  <% const headPath='../../partials/htmlHeadUser.ejs' ; %>
    <%- include(headPath, {csspath: '/css/userCss/purchase/orderFailurePage.css' }) %>
      <title>Order Failed | Essentials</title>
</head>

<body class="d-flex flex-column min-vh-100 bg-light">
  <%- include("../../partials/userNavbar.ejs") %>

    <main class="flex-grow-1 py-5 mt-5">
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-12 col-md-8 col-lg-6">
            <div class="card border-0 shadow-lg text-center p-4 p-md-5" style="border-radius: 30px;">
              <div class="crossmark-circle mb-4">
                <svg class="crossmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
                  <circle class="crossmark-circle-svg" cx="26" cy="26" r="25" fill="none" />
                  <path class="crossmark-line crossmark-line1" d="M16 16 L36 36" />
                  <path class="crossmark-line crossmark-line2" d="M36 16 L16 36" />
                </svg>
              </div>

              <h1 class="fw-bold mb-3" style="color: #e74c3c; font-size: 2.5rem;">Payment Failed</h1>
              <p class="text-muted mb-4 fs-5">Unfortunately, your payment could not be processed at this time.</p>

              <div class="alert alert-warning rounded-4 mb-4 text-start border-0 shadow-sm"
                style="background-color: #fff9eb;">
                <i class="fas fa-info-circle me-2 text-warning"></i>
                Don't worry! If any amount was deducted, it will be refunded automatically, or you can check your order
                status to retry the payment.
              </div>

              <div class="d-flex flex-column flex-sm-row gap-3 justify-content-center mt-4">
                <a href="/home" class="btn btn-outline-secondary btn-lg rounded-pill px-5">Home</a>
                <a href="/orders" class="btn btn-primary btn-lg rounded-pill px-5">Check Orders</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </main>

    <%- include("../../partials/userFooter.ejs") %>

      <script>
        document.addEventListener('DOMContentLoaded', () => {
          if (typeof ErrorToast === 'function') {
            ErrorToast("Payment Failed. Please try again.");
          }
        });
      </script>
</body>

</html>